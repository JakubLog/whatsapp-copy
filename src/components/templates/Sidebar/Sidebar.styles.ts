import styled from 'styled-components';

export const Wrapper = styled.div`
  display: grid;
  grid-template-columns: 100%;
  overflow: hidden;
  grid-template-rows: 55px 50px auto;
  border-right: 1px solid #ccc;
  position: relative;
`;
